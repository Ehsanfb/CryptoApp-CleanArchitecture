package com.example.cryptoapp.presentation.coin_list

/*
class CoinsViewModelFactory(private val repository: MainRepository) : ViewModelProvider.Factory {
    override fun <T : ViewModel> create(modelClass: Class<T>): T {
        if (modelClass.isAssignableFrom(CoinsViewModel::class.java)) {
            @Suppress("UNCHECKED_CAST")
            return CoinsViewModel(repository) as T
        }
        throw IllegalArgumentException("Unknown ViewModel class")
    }
}*/
